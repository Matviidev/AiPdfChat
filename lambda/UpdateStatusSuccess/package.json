{
  "name": "extracttextfroms3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:lambda": "node build.mjs && cd dist && zip ../function.zip index.js && cd .. && rm -r dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.958.0",
    "@aws-sdk/lib-dynamodb": "^3.958.0",
    "aws-sdk": "^2.1693.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
